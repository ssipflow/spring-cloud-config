apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: spring-cloud-config
  labels:
    app: spring-cloud
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-cloud
  template:
    metadata:
      labels:
        app: spring-cloud
        version: latest
    spec:
      containers:
      - name: spring-cloud-config
        image: hubtea/spring-cloud-config:latest
        ports:
        - name: http
          containerPort: 8888
